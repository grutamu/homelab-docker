{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":dependencyDashboard",
    ":semanticCommits",
    "docker:enableMajor"
  ],
  "timezone": "America/Chicago",
  "schedule": ["every weekend"],
  "prHourlyLimit": 0,
  "prConcurrentLimit": 10,
  "labels": ["renovate"],
  "automerge": true,
  "automergeType": "pr",
  "description": [
    "rebaseWhen=conflicted, not the default 'auto'. With automerge on, 'auto' means",
    "'behind-base-branch': every merge to main leaves the other open PRs behind, so",
    "Renovate force-pushes all of them, re-runs their checks, and blocks automerge",
    "until each goes green again. Draining a queue of N PRs then costs ~N^2/2",
    "force-pushes and notifications, and merges one PR per Renovate run. These stacks",
    "touch disjoint files and never actually conflict, so rebasing buys nothing."
  ],
  "rebaseWhen": "conflicted",
  "enabledManagers": ["docker-compose"],
  "docker-compose": {
    "managerFilePatterns": [
      "/(^|/)docker-compose\\.ya?ml$/"
    ]
  },
  "packageRules": [
    {
      "description": "Never automerge major versions",
      "matchUpdateTypes": ["major"],
      "automerge": false
    },
    {
      "description": "Postgres major upgrades require a manual pg_dump/restore migration — recreating on a new major breaks the data dir. Don't propose them.",
      "matchDatasources": ["docker"],
      "matchPackageNames": ["/(^|/)postgres$/"],
      "matchUpdateTypes": ["major"],
      "enabled": false
    },
    {
      "description": "Require manual review for pocket-id (auth provider — outage locks out all services)",
      "matchFileNames": ["pocket-id/**"],
      "groupName": "pocket-id stack",
      "automerge": false
    },
    {
      "description": "Require manual review for immich (postgres DB migrations can be irreversible)",
      "matchFileNames": ["immich/**"],
      "groupName": "immich stack",
      "automerge": false
    },
    {
      "description": "Require manual review for traefik majors (gateway — outage takes down everything)",
      "matchFileNames": ["traefik/**"],
      "groupName": "traefik stack"
    },
    {
      "description": "Group all monitoring stack updates",
      "matchFileNames": ["monitoring/**"],
      "groupName": "monitoring stack"
    },
    {
      "description": "Group all mediaserver stack updates",
      "matchFileNames": ["mediaserver/**"],
      "groupName": "mediaserver stack"
    },
    {
      "description": "Group all paperless updates",
      "matchFileNames": ["paperless/**"],
      "groupName": "paperless stack"
    },
    {
      "description": "Group all netbox updates",
      "matchFileNames": ["netbox/**"],
      "groupName": "netbox stack"
    },
    {
      "description": "Group all frigate updates",
      "matchFileNames": ["frigate/**"],
      "groupName": "frigate stack"
    },
    {
      "description": "Group 1Password Connect updates",
      "matchFileNames": ["1password/**"],
      "groupName": "1password stack"
    },
    {
      "description": "Shelfarr ships ~2 releases/day — batch into a single monthly PR instead of one per weekend run",
      "matchFileNames": ["shelfarr/**"],
      "groupName": "shelfarr stack",
      "schedule": ["before 6am on the first day of the month"]
    },
    {
      "description": "Require manual review for mcpjungle. It spawns upstream MCP servers as child processes with credentials, and its predecessor leaked subprocesses badly enough to OOM the host — bumps get eyes on them. Note the image tag carries a -stdio suffix Renovate must preserve; the plain tag cannot spawn stdio servers at all.",
      "matchFileNames": ["mcpjungle/**"],
      "groupName": "mcpjungle stack",
      "automerge": false
    },
    {
      "description": "Require manual review for hermes-agent. It's an autonomous agent with code execution and a self-modifying skill/plugin tree, and its dashboard is a credential surface — bumps get eyes on them. Tags are calver (v2026.8.3), so no update is ever classified major and the global major rule never applies here.",
      "matchFileNames": ["hermes-agent/**"],
      "groupName": "hermes-agent stack",
      "automerge": false
    },
    {
      "description": "Never open digest-only PRs — upstream image rebuilds create constant churn with no version change. All images are pinned to real tags.",
      "matchUpdateTypes": ["digest"],
      "enabled": false
    },
    {
      "description": "Ignore stable/release pseudo-tags (Frigate)",
      "matchCurrentValue": "/^(stable|release)$/",
      "enabled": false
    }
  ]
}
