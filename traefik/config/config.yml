http:
 #region routers 
  routers:
    proxmox:
      entryPoints:
        - "https"
      rule: "Host(`proxmox.calzone.zone`)"
      middlewares:
        - default-headers
        - https-redirectscheme
      tls: {}
      service: proxmox
    photos:
      entryPoints:
        - "https"
      rule: "Host(`photos.calzone.zone`)"
      middlewares:
        - default-headers
        - https-redirectscheme
      tls: {}
      service: photos
    frigate:
      entryPoints:
        - "https"
      rule: "Host(`frigate.calzone.zone`)"
      middlewares:
        - default-headers
        - https-redirectscheme
      tls: {}
      service: frigate
  
#endregion
#region services
  services:
    proxmox:
      loadBalancer:
        servers:
          - url: "https://192.168.10.11:8006"
        passHostHeader: true
    photos:
      loadBalancer:
        servers:
          - url: "http://192.168.99.42:2283"
        passHostHeader: true
    frigate:
      loadBalancer:
        servers:
          - url: "http://192.168.99.42:5000"
        passHostHeader: true
#endregion
  middlewares:
    https-redirectscheme:
      redirectScheme:
        scheme: https
        permanent: true
    default-headers:
      headers:
        frameDeny: true
        browserXssFilter: true
        contentTypeNosniff: true
        forceSTSHeader: true
        stsIncludeSubdomains: true
        stsPreload: true
        stsSeconds: 15552000
        customFrameOptionsValue: SAMEORIGIN
        customRequestHeaders:
          X-Forwarded-Proto: https

    default-whitelist:
      ipAllowList:
        sourceRange:
        - "10.0.0.0/8"
        - "192.168.0.0/16"
        - "172.16.0.0/12"

    secured:
      chain:
        middlewares:
        - default-whitelist
        - default-headers