services:
  traefik:
    image: traefik:v3.7.10
    container_name: traefik
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    networks:
      - proxy
    ports:
      - 80:80
      - 443:443
      # - 443:443/tcp # Uncomment if you want HTTP3
      # - 443:443/udp # Uncomment if you want HTTP3
    environment:
      CF_DNS_API_TOKEN: ${CF_DNS_API_TOKEN}
      OIDC_CLIENT_ID: ${OIDC_CLIENT_ID}
      OIDC_CLIENT_SECRET: ${OIDC_CLIENT_SECRET}
      FORWARD_AUTH_SECRET: ${FORWARD_AUTH_SECRET}
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./config/traefik.yml:/traefik.yml:ro
      # - ./config/acme.json:/acme.json
      - type: bind
        source: ./config/acme.json
        target: /acme.json
      - ./config/config.yml:/config.yml:ro
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.traefik.entrypoints=http"
      - "traefik.http.routers.traefik.rule=Host(`traefik-dashboard.calzone.zone`)"
      - "traefik.http.middlewares.traefik-https-redirect.redirectscheme.scheme=https"
      - "traefik.http.middlewares.sslheader.headers.customrequestheaders.X-Forwarded-Proto=https"
      - "traefik.http.routers.traefik.middlewares=traefik-https-redirect"
      - "traefik.http.routers.traefik-secure.entrypoints=https"
      - "traefik.http.routers.traefik-secure.rule=Host(`traefik-dashboard.calzone.zone`)"
      - "traefik.http.routers.traefik-secure.middlewares=pocket-id-auth@file"
      - "traefik.http.routers.traefik-secure.tls=true"
      - "traefik.http.routers.traefik-secure.tls.certresolver=cloudflare"
      - "traefik.http.routers.traefik-secure.tls.domains[0].main=calzone.zone"
      - "traefik.http.routers.traefik-secure.tls.domains[0].sans=*.calzone.zone"
      - "traefik.http.routers.traefik-secure.service=api@internal"

networks:
  # Traefik owns the shared ingress network.
  #
  # Ten other stacks attach to `proxy` with `external: true`, which means
  # something has to create it first. Until now that something was a
  # `docker network create proxy` typed by hand in Dec 2024 and recorded
  # nowhere -- so a rebuilt host would fail on the very first stack with
  # "network proxy declared as external, but could not be found".
  #
  # Defining it here makes a cold boot work, and puts the network next to the
  # thing that actually defines the ingress.
  #
  # `name: proxy` pins the real network name. Without it compose would create
  # `traefik_proxy` and every `external: true` reference elsewhere would miss.
  #
  # Note: `docker compose down` on this stack now attempts to remove the
  # network. That fails harmlessly while other stacks are still attached.
  proxy:
    name: proxy
    driver: bridge