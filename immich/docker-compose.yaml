name: immich
services:
  immich-server:
    container_name: immich_server
    image: ghcr.io/immich-app/immich-server:v3.0.3
    user: 1000:1000
    devices:
      - /dev/dri:/dev/dri
    # /dev/dri/renderD128 is `crw-rw---- root render`, and uid 1000 is not in
    # `render` -- so without this the container maps the device in and then
    # cannot open it. Hardware transcoding silently falls back to software.
    #
    # Numeric because the group name does not resolve inside the image. 993 is
    # the `render` gid on the host (and is pinned to that value in the NixOS
    # config, so it survives the migration).
    group_add:
      - "993"
    volumes:
      - nfs-photos:/usr/src/app/upload
      - /etc/localtime:/etc/localtime:ro
    environment:
      DB_PASSWORD:
      DB_USERNAME:
      DB_DATABASE_NAME: immich
    depends_on:
      - redis
      - database
    restart: always
    healthcheck:
      disable: false
    networks:
      - proxy
      - immich
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=proxy"
      - "traefik.http.routers.immich.rule=Host(`photos.calzone.zone`)"
      - "traefik.http.routers.immich.entrypoints=https"
      - "traefik.http.routers.immich.tls=true"
      - "traefik.http.services.immich.loadbalancer.server.port=2283"

  immich-machine-learning:
    container_name: immich_machine_learning
    image: ghcr.io/immich-app/immich-machine-learning:v3.0.3
    volumes:
      - model-cache:/cache
    environment:
      DB_PASSWORD:
      DB_USERNAME:
      DB_DATABASE_NAME: immich
    restart: always
    healthcheck:
      disable: false
    networks:
      - immich

  redis:
    container_name: immich_redis
    image: docker.io/redis:8.8-alpine
    healthcheck:
      test: redis-cli ping || exit 1
    restart: always
    networks:
      - immich

  database:
    container_name: immich_postgres
    image: ghcr.io/immich-app/postgres:16-vectorchord0.4.3-pgvectors0.2.0
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_DB: immich
      POSTGRES_INITDB_ARGS: '--data-checksums'
    volumes:
      - /docker-data/immich/postgres:/var/lib/postgresql/data
    restart: always
    networks:
      - immich

volumes:
  model-cache:

  nfs-photos:
    driver_opts:
      type: "nfs"
      o: "addr=truenas.calzone.zone,nolock,soft,nfsvers=4"
      device: ":/mnt/ssd-pool/photos"

networks:
  immich:
  proxy:
    external: true
